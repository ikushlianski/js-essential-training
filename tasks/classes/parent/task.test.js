import {goGetMoney_solution} from './task.solution';

describe('', () => {
  it('suggested solution', () => {
    expect(goGetMoney_solution()).toBe(1000)
  });

  it('your solution', () => {
    expect(goGetMoney()).toBe(1000)
  });
});
