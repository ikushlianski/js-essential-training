/**
 * Task
 * Write a sum function accepting an array of numbers and returning the sum of those numbers
 *
 * If a function is passed anything else, return 0
 */
export const sum = (arr) => {
    return;
};
